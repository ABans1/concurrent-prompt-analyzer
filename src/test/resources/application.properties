# Disable the write-ahead journal during tests so the suite doesn't create data/ files
# or attempt startup recovery. Journal behavior itself is covered by dedicated unit tests.
analyzer.journal.enabled=false
analyzer.journal.recover-on-startup=false
